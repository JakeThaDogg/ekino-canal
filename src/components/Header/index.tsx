import React from 'react';
import styled from 'styled-components';

const Title = styled.h1`
    color: #FFF;
    text-align: center;
`

const Header = () => {

    return (
        <header>
            <Title>Super Watch Search Engine 3000</Title>
        </header>
    )
}

export default Header;